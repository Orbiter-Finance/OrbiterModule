{
  "name": "bufeifpv-cms",
  "private": true,
  "version": "0.2.0",
  "scripts": {
    "dev:backend": "yarn --cwd backend dev",
    "dev:frontend": "wait-on http://localhost:3002 && yarn --cwd frontend dev",
    "dev": "cross-env FORCE_COLOR=1 npm-run-all -l -p dev:*"
  },
  "devDependencies": {
    "cross-env": "7.0.3",
    "npm-run-all": "4.1.5",
    "typescript": "^4.5.2",
    "wait-on": "5.2.1"
  },
  "dependencies": {}
}
